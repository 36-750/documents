2 inc inc inc   ;; do some math
dup 0 gt        ;; condition
    end dec dec dec begin   ;; true case
    end dup dup inc begin   ;; false case
ifelse
'result
print
pop
print           ;; what's the result

